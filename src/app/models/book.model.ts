export interface IBook {
  title: string;
  author: string;
  year: number;
  userId: string;
}

export interface IBookId {
  id: number;
  title: string;
  author: string;
  year: number;
  userId: string;
}
